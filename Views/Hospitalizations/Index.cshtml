@model IEnumerable<ClinicaCaniZzoo.Models.Hospitalizations>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Animals.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatoRicovero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FotoAnimale)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Animals.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatoRicovero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FotoAnimale)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRicovero }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRicovero }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRicovero })
        </td>
    </tr>
}

</table>
